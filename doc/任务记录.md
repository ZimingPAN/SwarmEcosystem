# 任务记录

## 本轮已完成

### baseline-audit

- 核验了 RLKMC 的观测、奖励、`delta_t`、PPO 路径。
- 确认 `reward = delta_E * reward_scale`，`delta_t = -log(r) / total_rate`。
- 确认 `SGDNTC_Model` 是当前实际在用的图式编码器，`VacancyEmbedding` 中传统 GAT 路径未启用。

### baseline-fix

- 修复 `services.__init__` 循环导入。
- 更新 `Worker` 相关测试到新接口。
- 新增共享观测 codec 单测。

### lightzero-kmc-adapter

- 新增 `kmc_graph` 模型类型。
- 新增 LightZero 版本 KMC 环境包装器。
- 新增 KMC 的 Gumbel MuZero 配置样例。
- 新增 `time_head`。

### dreamer4-graph-adapter

- 新增 `KMCGraphEncoder`。
- 新增 `KMCDynamicsWorldModel`。
- 新增 `time_head`。

## 本轮验证

- `python -m pytest -q tests/test_unit_compute_engine.py tests/test_unit_worker.py tests/test_unit_observation_codec.py`
  - 结果：通过
- `python -m py_compile ...`
  - 结果：通过
- 远端导入 smoke test
  - `lzero.model.kmc_graph_muzero_model`：通过
  - `zoo.kmc.envs.kmc_lightzero_env`：通过
  - `dreamer4.kmc`：通过
- 远端最小前向 smoke test
  - `KMCLightZeroEnv.reset()`：通过
  - `KMCGraphMuZeroModel.initial_inference()`：通过
  - `KMCGraphEncoder.forward()`：通过
- 远端最小训练 smoke test
  - `KMCGraphMuZeroModel`：单步反向传播通过
  - `KMCDynamicsWorldModel`：实例化、编码、`predict_time_delta()` 通过
- 远端脚本级 smoke test
  - `kmc_gumbel_muzero_smoke.py`：3 步通过，raw reward 当前为 0，loss 下降
  - `train_kmc_smoke_test.py`：3 步通过，raw reward 当前为 0，`dt_mean` 与 loss 下降
- 远端短 rollout
  - raw reward 已恢复为原始值
  - 当前 30 步随机 rollout：`min=0, max=0, mean=0`
  - 说明“平均 reward 为正”的真实目标尚未完成
- defect graph 输入
  - world-model 已从“最近 `16` 个 defect”修正为“**16 个 BCC 配位壳层**内的 defect 图编码”
  - 当前固定容量上限为 `384` 个 defect 节点，超出后按距离裁剪
  - 节点属性：到当前 vacancy agent 的 `offset + type`
  - 边属性：defect 间 offset（encoder 内部动态构造）
- reward 链路修复
  - 第一层修复：去掉 `calculate_system_energy()` 中硬编码 `return 0`
  - 第二层修复：发现 `init_lattice()` 实际并不维护 `self.lattice`，且 `self.coords` 也未初始化，导致能量函数仍会早退为 0
  - 已改为：
    - `init_lattice()` 中初始化全 BCC 晶格坐标 `self.coords`
    - `calculate_system_energy()` 不再依赖 `self.lattice`，而是基于 `vac_pos_set / cu_pos_set` 与 `_batch_get_type_from_coords()` 做真实系统能量统计
  - 修复后，本地与远端都已恢复真实正奖励动作：
    - 本地全动作枚举：`48` 个合法动作中 `7` 个非零，最大奖励约 `0.16`
    - 远端全动作枚举：`48` 个合法动作中 `1` 个非零，最大奖励约 `0.16`
- PPO baseline 链路修复完成
  - 已修复：
    - `train.py` 无条件绑 GPU
    - `lattice_v_nums/lattice_cu_nums=0` 时无法回退到 density
    - `reset()` 默认不给 `V_features_local`
    - runner/buffer 仍使用 parser 中的 `lattice_v_nums=0`
    - reward 为标量 float 时 buffer `.copy()` 崩溃
    - actor 内部 `diff_k/dist_k` 原地写入导致反向传播版本冲突
  - 当前本地 probe 结果：
    - 8-step probe：训练完成，`AvgReward = 0.1600`
    - 500-step probe：5 个 episode 全部跑通，`AvgReward` 在 `0.16 / 0 / -0.16 / 0` 附近波动，整体说明 baseline 已可稳定对比，但尚无明显正收益优势
- MuZero oracle-greedy / BC warm start
  - 修复 `KMCLightZeroEnv` 中 `total_half_sites` 误写为 `prod(lattice_size)//2` 的错误；BCC 实际应为 `prod(lattice_size)*2`
  - 修复后，在 `20^3` 本地 LightZero KMC wrapper 中：
    - vacancy 数从错误的 `1` 恢复到 `3`
    - 合法动作数从 `8` 恢复到 `24`
  - 新增 `LightZero-main/zoo/kmc/config/kmc_muzero_oracle_bc_probe.py`
    - 每步枚举合法动作、通过 `deepcopy(env._env)` 计算 oracle-greedy teacher
    - 收集 defect graph 观测 + 最优动作 + `delta_t`
    - 对 `KMCGraphMuZeroModel` 做 policy CE + `delta_t` 回归 warm start
  - 本地 `20^3` 小数据 probe：
    - oracle 数据集平均单步 reward 已为正：`0.0025 ~ 0.02`
    - 但 16 壳层 / 384 defect 图编码在 CPU 上前后向过慢，后续必须切到服务器 GPU + tmux 跑正式验证
  - 远端 GPU/tmux 验证（`20^3`, `16 shells`, compact capacity=`max_vacancies=4`, `max_defects=96`）：
    - PPO 500-step 对照：`AvgReward` 最终约 `0.0000`，整体仍在 `0` 附近波动
    - MuZero weighted oracle-BC：
      - `dataset_size=128`
      - `oracle_avg_reward=0.020000`
      - `positive_ratio=0.250000`
      - 训练后：`eval_avg_reward=0.020000`
      - `positive_step_ratio=0.250000`
      - 说明 MuZero 世界模型路径已经在**原始 raw reward 口径**下超过 PPO 基线
  - 关键修正：
    - oracle-BC 脚本改为真正使用 GPU
    - 采集/评估不再复用已演化环境，而是从初始原型环境 `deepcopy`
    - 对正奖励样本增加权重，避免模型塌缩到“全选零奖励动作”
- Dreamer4 oracle-greedy / BC probe
  - 新增 `dreamer4-main/train_kmc_oracle_bc_probe.py`
    - 复用 `KMCDynamicsWorldModel` 作为 GNN/graph 编码器与 `time_head`
    - 额外接一个轻量 policy head，对 oracle 动作做加权 BC
  - 远端 GPU/tmux 验证（同口径 `20^3`, `16 shells`, `max_vacancies=4`, `max_defects=96`）：
    - `dataset_size=128`
    - `oracle_avg_reward=0.020000`
    - `positive_ratio=0.250000`
    - 训练后：`eval_avg_reward=0.020000`
    - `positive_step_ratio=0.250000`
  - 说明 Dreamer4 路径也已经在**原始 raw reward** 下拿到正平均回报，并高于 PPO 基线
- `40^3` / `2NN-4NN` / 容量扫描
  - 首轮 `40^3` compact probe（`max_vacancies=4`, `max_defects=96`）：
    - MuZero：`eval_avg_reward=0.020000`
    - Dreamer：`eval_avg_reward=0.020000`
  - 之后发现 `neighbor_order` 虽然从配置传入了 env/model，但**并没有真正影响 defect graph 的边连接**：
    - `build_defect_graph_observation()` 只按 `16 shells` 选节点
    - `DefectGraphEncoder` 对 defect-defect 边默认做全连接 attention
    - 所以最早一轮 `2NN/4NN` 结果完全一致，属于“假对照”
  - 已修复：
    - 在 `DefectGraphEncoder` 中新增 `neighbor_order` 连边半径控制
    - 用 BCC shell cutoff 将 `2NN/4NN` 转成真实的 edge mask
    - MuZero / Dreamer / probe 脚本 / LightZero 配置已全部把该参数传到模型侧
  - 服务器快速验证：
    - MuZero `edge_cutoff_sq`: `2NN=4.0`, `4NN=11.0`
    - Dreamer `edge_cutoff_sq`: `2NN=4.0`, `4NN=11.0`
    - Dreamer latent 的 `L1` 差异已非零，说明编码确实发生变化
  - 修正后重新跑 `40^3` 真正对照：
    - `2NN`
      - MuZero：`eval_avg_reward=0.020000`
      - Dreamer：`eval_avg_reward=0.020000`
    - `4NN`
      - MuZero：`eval_avg_reward=0.000000`
      - Dreamer：`eval_avg_reward=0.020000`
    - 结论：Dreamer 对更宽边半径更稳；MuZero 在 `4NN` 下当前配置出现退化
  - 容量扫描（服务器 GPU，`40^3`, `2NN`, batch=2 前后向）：
    - `4 x 96`: 约 `0.21 GB`
    - `8 x 96`: 约 `0.40 GB`
    - `8 x 192`: 约 `1.48 GB`
    - `16 x 192`: 约 `2.95 GB`
    - `8 x 384`: 约 `5.81 GB`
    - `16 x 384`: 约 `11.60 GB`
    - `32 x 384`: 约 `23.17 GB`
    - 说明在修正成真实 `2NN` 图之后，之前的 OOM 判断已经失效，`32 x 384` 现在是可运行的
  - 结果汇总表：

    | 模型 | 晶格 | 邻域 | 图容量 | oracle_avg_reward | eval_avg_reward | positive_step_ratio |
    | --- | --- | --- | --- | ---: | ---: | ---: |
    | MuZero | `20^3` | `2NN` | `4 x 96` | 0.020000 | 0.020000 | 0.250000 |
    | Dreamer | `20^3` | `2NN` | `4 x 96` | 0.020000 | 0.020000 | 0.250000 |
    | MuZero | `40^3` | `2NN` | `4 x 96` | 0.020000 | 0.020000 | 0.125000 |
    | Dreamer | `40^3` | `2NN` | `4 x 96` | 0.020000 | 0.020000 | 0.125000 |
    | MuZero | `40^3` | `4NN` | `4 x 96` | 0.020000 | 0.000000 | 0.125000 |
    | Dreamer | `40^3` | `4NN` | `4 x 96` | 0.020000 | 0.020000 | 0.125000 |

## 当前阻塞

- MuZero / Dreamer 还未进入更长程、更大晶格（尤其 `40^3`）的正式训练
- `use_system_stats=False` 当前是为了绕开远端 `scipy` 缺失，后续要决定是否补齐 `scipy` 并恢复真实系统统计
- `4NN` 下 MuZero 当前退化到 `eval_avg_reward=0.000000`，需要针对性调参
- 还没有把 `32 x 384` 带入正式 oracle-BC / 更长 horizon，只做了显存探针

## 下轮优先事项

1. 将 `32 x 384` 带入正式 oracle-BC / 更长 horizon。
2. 针对 `4NN` 下 MuZero 退化做专项调参。
3. 推进 `delta_t` 更深地接入正式训练目标。
4. 继续推进正式 world-model 训练，而不止于 oracle-supervised warm start。
